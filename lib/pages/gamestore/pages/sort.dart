import 'package:flutter/material.dart';
import 'categories.dart';

class SortPage extends StatelessWidget {
  const SortPage({super.key});
  @override
  Widget build(BuildContext context) => const CategoriesPage();
}
